###############################################################################
# PLANTILLA de secretos para la VM de BD.
#
# 1. Copia a:  group_vars/db_secrets.yml   (está en .gitignore, NO se sube)
# 2. (Recomendado) cífralo:  ansible-vault encrypt group_vars/db_secrets.yml
#    y ejecuta los playbooks con  --ask-vault-pass
###############################################################################

vault_auth_db_password: "CAMBIAME"
vault_subscription_db_password: "CAMBIAME"
vault_catalog_db_password: "CAMBIAME"
vault_rating_db_password: "CAMBIAME"
vault_fx_db_password: "CAMBIAME"
vault_history_db_password: "CAMBIAME"
vault_notification_db_password: "CAMBIAME"
vault_redis_password: "CAMBIAME"

# Opcional: sólo si las imágenes de DockerHub son privadas (para `docker login`).
# Déjalo vacío si las imágenes son públicas.
vault_dockerhub_token: ""
