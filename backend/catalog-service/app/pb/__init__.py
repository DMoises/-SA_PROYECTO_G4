# Paquete de stubs gRPC generados desde proto/catalog.proto
# (catalog_pb2.py y catalog_pb2_grpc.py). Se generan con ./generate.sh en
# local o automaticamente durante el build de Docker; no se versionan.
