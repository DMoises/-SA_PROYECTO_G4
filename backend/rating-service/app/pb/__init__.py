# Paquete de stubs gRPC generados desde proto/rating.proto
# (rating_pb2.py y rating_pb2_grpc.py). Se generan con ./generate.sh en local
# o automaticamente durante el build de Docker; no se versionan.
