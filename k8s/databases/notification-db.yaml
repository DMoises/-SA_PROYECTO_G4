apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-db
  namespace: quetxal-tv-prod
  labels:
    app: notification-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-db
  template:
    metadata:
      labels:
        app: notification-db
    spec:
      containers:
      - name: postgres
        image: tu-registro/quetxal-notification-db:latest
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: quetxal-secrets
              key: NOTIFICATION_DB_NAME
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: quetxal-secrets
              key: NOTIFICATION_DB_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: quetxal-secrets
              key: NOTIFICATION_DB_PASSWORD
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "$(POSTGRES_USER)", "-d", "$(POSTGRES_DB)"]
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          exec:
            command: ["pg_isready", "-U", "$(POSTGRES_USER)", "-d", "$(POSTGRES_DB)"]
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: notification-db
  namespace: quetxal-tv-prod
spec:
  type: ClusterIP
  selector:
    app: notification-db
  ports:
  - port: 5432
    targetPort: 5432
